{
  "name": "mock-db",
  "version": "1.0.0",
  "description": "",
  "main": "db.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db": "json-server --watch dev.json --middlewares server.js --routes routes.json",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2",
    "jsonwebtoken": "^8.4.0",
    "jwt-decode": "^2.2.0"
  },
  "devDependencies": {
    "@types/node": "^10.12.18"
  }
}
