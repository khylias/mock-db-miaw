{
  "name": "mock-db",
  "version": "1.0.0",
  "description": "",
  "main": "db.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node db.ts",
    "db": "json-server --watch db.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.2"
  },
  "devDependencies": {
    "@types/node": "^10.12.18"
  }
}
